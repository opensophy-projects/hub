---
title: "Плазменная волна"
description: UI Компонент. Анимированный фоновый эффект плазменная волна.
date: 2026-05-18
tags: разработка, ui, ui-компоненты
keywords: react background, animated background, plasma-wave, shader
robots: index, follow
lang: ru
---

[uic:plasma-wave]

Декоративный фон **Плазменная волна** для hero-блоков, секций и лендингов. Оформлен в едином формате как страница «Темная вуаль»: с зависимостями, использованием, рекомендациями и полным исходным кодом.

---

## Стек и зависимости

- `react`
- Дополнительные зависимости берутся из исходного файла компонента (см. код ниже).

## Использование

```tsx
import PlasmaWave from '@/features/ui-components/plasma-wave/plasma-wave';

<div style={{ width: '100%', height: '420px' }}>
  <PlasmaWave />
</div>
```

:::note
Компонент занимает весь размер родительского контейнера. Задайте контейнеру явные размеры (`height`, `min-height` и т.д.).
:::

## Оригинальный код

```tsx
// Файл реализации компонента отсутствует в репозитории. См. viewer/config.
```

## Пропсы

Список и дефолтные значения пропсов задаются прямо в исходнике компонента (см. секцию «Оригинальный код") и/или в `config.json` этого компонента в UI viewer.

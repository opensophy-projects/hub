---
import Layout from '@/app/layouts/Layout.astro';
import DocContent from '@/features/docs/components/DocContent';
import fs from 'node:fs';
import path from 'node:path';
import { buildDocFromPath } from '../../../scripts/docUtils.mjs';

const docsDir = path.join(process.cwd(), 'Docs');
const welcomeMdPath = path.join(docsDir, 'welcome.md');

// Always read fresh from disk (both dev and prod for index)
let welcomeDoc = null;
if (fs.existsSync(welcomeMdPath)) {
  welcomeDoc = buildDocFromPath(welcomeMdPath, docsDir);
  welcomeDoc.id = 'welcome';
  welcomeDoc.slug = '';
  if (!welcomeDoc.keywords) {
    welcomeDoc.keywords = 'Opensophy, документация, искусственный интеллект, кибербезопасность, ИИ, AI, security, статьи, блог, новости';
  }
}

const title       = welcomeDoc?.title       || 'Hub - Opensophy';
const description = welcomeDoc?.description || 'Hub проекта Opensophy.';
const keywords    = welcomeDoc?.keywords    || 'Opensophy, документация';
---

<Layout
  title={title}
  description={description}
  url="/"
  type="website"
  keywords={keywords}
  robots="index, follow"
>
  {welcomeDoc ? (
    <DocContent client:only="react" doc={welcomeDoc} />
  ) : (
    <div style="min-height: 100vh; display: flex; align-items: center; justify-content: center;">
      <p>Загрузка...</p>
    </div>
  )}
</Layout>
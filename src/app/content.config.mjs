// Astro ищет content.config.mjs в srcDir (src/app/).
// Проект не использует Content Collections — коллекции пустые,
// но файл нужен чтобы убрать [WARN] Content config not loaded.
export const collections = {};